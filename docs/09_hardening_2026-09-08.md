# Hardening 2026-09-08 — Laporan Tes & Perbaikan

> Ponytail Full — minimal diff, no over-engineering.
> Penguji: Hermes Agent | VPS 43.134.168.66:1980 | Laravel 13.29.0

## 1. Ringkas Tes

### User E2E (23 skenario) — LULUS semua
- Live: / 200, /register 200, /login 200, /tema 5 tema 49k, /u/adit-zatid 200, /up 200, /orders/uuid tanpa login 302
- Register/login: ponytail id 3 verified ke /dashboard; duplicate diblokir
- Checkout 4 varian: 45d 54.390, lifetime 109.890, assisted 82.140, coupon 30% 38.073 — metadata benar
- Coupon MOMENINDAH: calc/cap/validasi (inactive/expired/max_uses/min_spend) benar; apply/remove 302 snap_token null
- Payment: simulate 302 success paid + unlock 45d/ lifetime idempotent; webhook capture pending settlement deny OK + ack fake order
- Views: pending 200, paid tanpa from 302 ke success, dengan from dashboard 200 invoice, success/invoice 200
- Invitation gate: canUseTheme yes/no, canUseThemeForInvitation after buy yes after 1 invite no (1 tema=1 undangan)
- AuthZ: order user 3 oleh user 2 403; public /u/adit-zatid 200 wishes 7

### Hacker (11 vektor)
Aman: IDOR 302/403, admin bypass 302/403, mass-assign whitelist, price tamper server-side gross, SQLi binding, XSS escaped 419, upload mimes max, CSRF except webhook, simulate 403.
VULN proven 2x (sudah fix):
- Webhook tanpa signature_key pending jadi paid gratis ORD-THM-A5AA0PLB
- Webhook gross 1 rupiah untuk order 54k tetap paid tanpa cek amount vs DB

## 2. Perbaikan (4 file, minimal diff)

- P0 WebhookController handle: jika isConfigured dan signature kosong 403 Missing signature; jika verify gagal 403 Invalid; jika round gross != round order.total_amount 400 Gross mismatch. Tambah ponytail strict gross comment.
- P1 CheckoutController applyCoupon: DB transaction + Coupon lockForUpdate, import DB facade.
- P1 routes web: POST orders coupon middleware throttle 10,1.
- P2 User fillable: hanya name email password, hapus role status package_id email_verified_at.
- P2 DB backfill: UPDATE user_themes expires_at = unlocked_at +45d where null (2 row).

Skipped: queue/job, Policy, const DURATION TAX, guard is_published — add when traffic >100 order/hari atau multi-gateway.

## 3. Verifikasi
- optimize clear + optimize -> 8 orders route, 3 hit orders.success, config/routes/views CACHED
- php -l semua file OK; manual webhook tanpa sig 403, gross mismatch 400, coupon throttle 429
- Order 8 direvert pending siap cek ulang

## 4. Cleanup test data (opsional)
DELETE FROM payments WHERE order_id IN (SELECT id FROM orders WHERE user_id IN (3,4));
DELETE FROM user_themes WHERE user_id IN (3,4);
DELETE FROM orders WHERE user_id IN (3,4);
DELETE FROM users WHERE id IN (3,4);

## 5. Next
- Seed system_settings/packages bila ingin quota partner terbatas (sekarang 0 = unlimited fallback).
- APP_DEBUG=false sudah benar.

# ponytail: 1 md di docs, no PDF lib baru
